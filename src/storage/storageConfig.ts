const nameApp = '@daily_diet';

const DATE_COLLECTION = `${nameApp}:date`;
const DATE_REF_MEAL_COLLECTION = `${nameApp}:listRef`;
const REF_MEAL = `${nameApp}:refMeal`;
const MEAL_COLLECTION = `${nameApp}:meal`;
const STATISTICS = `${nameApp}:statistics`;

export { MEAL_COLLECTION, STATISTICS, DATE_COLLECTION, REF_MEAL, DATE_REF_MEAL_COLLECTION };
